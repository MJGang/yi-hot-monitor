# Docker 环境变量默认值
# 此文件入仓库，包含非敏感默认值
# API Key 等敏感信息请在 backend/.env 中配置

# MySQL (仅 docker-compose 内部使用)
MYSQL_HOST=mysql
MYSQL_USER=root
MYSQL_PASSWORD=hotspot123
MYSQL_DATABASE=hotspot_db

# Backend
DATABASE_URL=mysql+aiomysql://root:hotspot123@mysql:3306/hotspot_db
